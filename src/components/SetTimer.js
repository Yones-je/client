import React from 'react'

const SetTimer = () => {
    return (
        <form onSubmit={}>
        <input type="number" placeholder="Minutes" onChange={} className="form__input-text" />
        <input type="submit" value="Set" className="form__submit" />
      </form>
    )
}

export default SetTimer
